- This module could be extended to add the external_api_endpoint model, with the purpose to store in the database the diferent urls of the same API we can call, and some configuration data or extra information.

- This module does have a custom logger that can print logs in your console or log file. You can manually enable/disable those logs (Explained in the USAGE section). However, developing an upgrade to simplify the log enable/disable whould be a nice improvement.
